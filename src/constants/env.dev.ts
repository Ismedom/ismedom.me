
export const CONFIG = {
    DEBUG: {
        ENABLED: true
    },
    FEATURES: {
        CHECKED: true,
        DISABLED: true,
        ENABLED: false
    },
    NETWORK: {
        MAX_RETRIES: 5,
        TIMEOUT_MS: 3000
    }
};